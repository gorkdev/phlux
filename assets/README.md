# Assets

Drop app icons here before running `electron-builder`:

- `icon.ico` — Windows (256×256 recommended, multi-size ICO)
- `icon.icns` — macOS
- `icon.png` — Linux (512×512)

Until real icons are added, Electron will use its default icon during `npm run dev`.
